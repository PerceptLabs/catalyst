{
  "name": "@aspect/catalyst",
  "version": "0.0.1",
  "description": "Catalyst — Browser-native runtime engine (Workers mode)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts"
  },
  "dependencies": {
    "@aspect/catalyst-core": "workspace:*",
    "@aspect/catalyst-workers": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
